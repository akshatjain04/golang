# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-processor.js"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tags"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.api.host"
      http:
        pool: 10
    prod:
      target: "http://prod.api.host"
      http:
        pool: 20

scenarios:
  - name: "Find pets by tags failure cases"
    flow:
      - log: "Invalid tag value"
        get:
          url: "/pet/findByTags?tags=invalidTag"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Invalid URL"
        get:
          url: "/pet/invalidURL?tags=tag1,tag2,tag3"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Unauthorized access"
        get:
          url: "/pet/findByTags?tags=tag1,tag2,tag3"
          headers:
            Authorization: "InvalidAuthToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1
