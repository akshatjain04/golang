# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_success.csv"
    fields:
      - "petId"
    skipHeader: true
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Delete a pet"
    flow:
      - log: "Deleting a pet"
      - delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "your_api_key"
          expect:
            - statusCode: 200
            - hasProperty: "message"
      - think: 1
