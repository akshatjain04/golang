# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/certainExternal/service/v1/Event/"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689771524.csv"
    fields:
      - "accountCode"
      - "eventCode"
      - "eventName"
      - "eventStatus"
      - "notes"
      - "startDate"
      - "endDate"
      - "timezone"
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            eventCode: "{{ eventCode }}"
            accountCode: "{{ accountCode }}"
            eventName: "{{ eventName }}"
            eventStatus: "{{ eventStatus }}"
            notes: "{{ notes }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            timezone: "{{ timezone }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            eventCode: ""
            accountCode: "{{ accountCode }}"
            eventName: "{{ eventName }}"
            eventStatus: "{{ eventStatus }}"
            notes: "{{ notes }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            timezone: "{{ timezone }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            eventCode: "{{ eventCode }}"
            accountCode: "{{ accountCode }}"
            eventName: "{{ eventName }}"
            eventStatus: "{{ eventStatus }}"
            notes: "{{ notes }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            timezone: "{{ timezone }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/{{ accountCode }}?fromTemplate={{ eventCode }}"
          json:
            eventCode: "{{ eventCode }}"
            accountCode: "{{ accountCode }}"
            eventName: "{{ eventName }}"
            eventStatus: "{{ eventStatus }}"
            notes: "{{ notes }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            timezone: "{{ timezone }}"
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            eventCode: "{{ eventCode }}"
            accountCode: "{{ accountCode }}"
            eventName: "{{ eventName }}"
            eventStatus: "{{ eventStatus }}"
            notes: "{{ notes }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            timezone: "{{ timezone }}"
          expect:
            - statusCode: 200
