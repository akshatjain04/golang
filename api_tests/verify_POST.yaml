# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Accept: "application/json"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/Auth/verify-otp"
          json:
            phone_number: "70652044334"
            otp: "5645"
            flag: "1"
          expect:
            - statusCode: 200
            - hasProperty: "Expected Property in Response" # Replace with actual property

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/Auth/verify-otp"
          json:
            phone_number: "70652044334"
            otp: ""
            flag: "1"
          expect:
            - statusCode: 400
            - hasProperty: "Expected Error Property in Response" # Replace with actual error property

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/Auth/verify-otp"
          json:
            phone_number: "70652044334"
            otp: "Special Characters or Large Data" # Replace with actual edge case
            flag: "1"
          expect:
            - statusCode: "{{Expected Status Code}}" # Replace with actual status code
            - hasProperty: "Expected Property in Response" # Replace with actual property
