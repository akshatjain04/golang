# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6e4c4bbafe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_others.csv"
    fields: ["orderId"]
    skipHeader: true

scenarios:
  - name: "Delete purchase order by ID - Failure cases"
    flow:
      - log: "Delete Order with invalid ID - Expect 400"
        post:
          url: "/store/order/abcd"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                - "description": "Invalid ID supplied"
        think: 2

      - log: "Delete Order with ID not found - Expect 404"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "description": "Order not found"
        think: 2

      - log: "Delete Order with invalid URL - Expect error"
        post:
          url: "/store/order_invalid/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 2

      - log: "Delete Order with invalid authorization - Expect error"
        post:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "Invalid_Token"
          expect:
            - statusCode: 401
        think: 2
