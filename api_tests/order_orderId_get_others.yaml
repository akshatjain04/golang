# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74f49e1ef7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Testing /store/order/{orderId} endpoint"
    flow:
      - log: "Sending request with invalid orderId"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 5

      - log: "Sending request with valid orderId but invalid token"
        get:
          url: "/store/order/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        think: 5

      - log: "Sending request with orderId not present in the system"
        get:
          url: "/store/order/999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5

      - log: "Sending request to invalid URL"
        get:
          url: "/store/invalid/{{ orderId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5
