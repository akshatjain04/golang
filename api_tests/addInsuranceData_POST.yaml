# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/addInsuranceData"
          headers:
            Authorization: "{{auth}}"
          formData:
            trip_id: "919"
            period: "p1"
            tripstarttimestamp: "1617314108485"
            tripendtimestamp: "1617314608485"
            insurance_type: "11"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - equals: 
                status: true
                status_code: 200
                message: "success"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/addInsuranceData"
          headers:
            Authorization: "{{auth}}"
          formData:
            trip_id: ""
            period: "p1"
            tripstarttimestamp: "1617314108485"
            tripendtimestamp: "1617314608485"
            insurance_type: "11"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/addInsuranceData"
          headers:
            Authorization: "{{auth}}"
          formData:
            trip_id: "919"
            period: "p1"
            tripstarttimestamp: "1617314108485"
            tripendtimestamp: "1617314608485"
            insurance_type: "11"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - equals: 
                status: true
                status_code: 200
                message: "success"
