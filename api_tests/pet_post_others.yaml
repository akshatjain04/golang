# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16cf30f894

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 200
  phases:
    - duration: 20
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "pet_post_others.csv"
    fields:
      - "id"
      - "category"
      - "name"
      - "photoUrls"
      - "tags"
      - "status"
    skipHeader: true

scenarios:
  - name: "Add a new pet to the store"
    flow:
      - log: "Invalid request: Missing required fields"
        post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: ""
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
      - think: 1
      - log: "Invalid request: Invalid URL"
        post:
          url: "/invalid_pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Invalid request: Invalid Authorization"
        post:
          url: "/pet"
          headers:
            Authorization: "Invalid Token"
            content-type: "application/json"
          json:
            id: "{{ id }}"
            category: "{{ category }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
      - think: 1
