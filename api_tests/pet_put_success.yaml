# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a21296b4f6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_put_success.csv"
    fields:
      - "id"
      - "category_id"
      - "category_name"
      - "name"
      - "photoUrls"
      - "tags_id"
      - "tags_name"
      - "status"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Pet update scenario"
    flow:
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            category:
              id: "{{ category_id }}"
              name: "{{ category_name }}"
            name: "{{ name }}"
            photoUrls: [ "{{ photoUrls }}" ]
            tags: 
              - id: "{{ tags_id }}"
                name: "{{ tags_name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
          log: "Update pet request"
        think: 1
