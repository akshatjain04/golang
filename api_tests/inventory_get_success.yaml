# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - "inventoryStatus"
      - "quantity"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Inventory status check"
    flow:
      - log: "Fetching inventory status"
      - get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "inventoryStatus"
            - hasProperty: "quantity"
          capture:
            - json: "$.inventoryStatus"
              as: "inventoryStatus"
            - json: "$.quantity"
              as: "quantity"
      - think: 1
      - log: "Checking inventory quantity"
      - get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "quantity"
          capture:
            - json: "$.quantity"
              as: "quantity"
      - think: 1
