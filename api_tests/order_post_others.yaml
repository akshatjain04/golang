# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=036bac41de

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_post_others.csv"
    fields: ["id", "petId", "quantity", "shipDate", "status", "complete"]
    skipHeader: true

scenarios:
  - name: "Place order API failure scenario"
    flow:
      - log: "Sending a request with invalid URL"
        post:
          url: "/store/orders"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending a request with invalid authorization token"
        post:
          url: "/store/order"
          headers:
            authorization: "invalid_token"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 401
        think: 1
      - log: "Sending a request with missing required fields"
        post:
          url: "/store/order"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            petId: "{{ petId }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Sending a request with invalid field values"
        post:
          url: "/store/order"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "invalid_id"
            petId: "{{ petId }}"
            quantity: "{{ quantity }}"
            shipDate: "{{ shipDate }}"
            status: "{{ status }}"
            complete: "{{ complete }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
