# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "data.csv"
    fields:
      - "auth"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/Accident/getAccidentlist"
          headers:
            Authorization: "{{ auth }}"
          expect:
            - statusCode: 200
            # Add additional assertions for expected data in the response body

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/Accident/getAccidentlist"
          headers:
            Authorization: "{{ auth }}"
          # Add invalid JSON payload
          json:
            invalidField: "invalidValue"
          expect:
            - statusCode: 400
            # Add assertion for expected error message in the response body

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/Accident/getAccidentlist"
          headers:
            Authorization: "{{ auth }}"
          # Add JSON payload with special characters or large data
          json:
            specialCharField: "!@#$%^&*()"
            largeDataField: "largeData"
          expect:
            - statusCode: "{{ expectedStatusCode }}"
            # Add assertion for expected pattern or behavior in the response body
