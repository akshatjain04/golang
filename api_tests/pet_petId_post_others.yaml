# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update pet with form data"
    flow:
      - log: "Test Case 1: Invalid URL"
        post:
          url: "/invalid/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 5

      - log: "Test Case 2: Missing petId in path"
        post:
          url: "/pet/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 5

      - log: "Test Case 3: Invalid Authorization"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "InvalidAuthToken"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 5

      - log: "Test Case 4: Missing formData"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          expect:
            - statusCode: 405
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 5

      - log: "Test Case 5: Invalid Content-Type"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 415
            - hasProperty: "message"
            - hasProperty: "error"
      - think: 5
