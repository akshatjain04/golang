# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  http:
    pool: 10
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_DELETE_1689771793.csv"
    fields:
      - "accountCode"
      - "eventCode"
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - delete: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
        expect:
          - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - delete: "/certainExternal/service/v1/Event/invalidAccountCode/invalidEventCode"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete: "/certainExternal/service/v1/Event/{{ accountCode }}/specialCharacterEventCode"
        expect:
          - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - delete: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}?param1=value1&param2=value2"
        expect:
          - statusCode: 200

  - name: "API request with authorization"
    flow:
      - delete: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
        expect:
          - statusCode: 200
