# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "inventory_get_others.csv"
    fields:
      - "invalidApiKey"
    skipHeader: true

scenarios:
  - name: "Negative test cases for /store/inventory API"
    flow:
      - log: "Test Case 1: Send request with invalid API key"
        get:
          url: "/store/inventory"
          headers:
            api_key: "{{ invalidApiKey }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

      - log: "Test Case 2: Send request to non-existent endpoint"
        get:
          url: "/store/nonexistent"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Test Case 3: Send request with invalid header"
        get:
          url: "/store/inventory"
          headers:
            invalid_header: "invalid_value"
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      - log: "Test Case 4: Send request without API key"
        get:
          url: "/store/inventory"
          expect:
            - statusCode: 401
            - hasProperty: "message"
