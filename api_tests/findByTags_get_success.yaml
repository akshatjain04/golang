# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c43742dac4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - "tags"
    skipHeader: true

scenarios:
  - name: "Find pets by tags"
    flow:
      - log: "Sending GET request to /pet/findByTags"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 200
            # Check if the response contains the necessary properties
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            # Check if the response contains a header
            - hasHeader: "content-type"
        think: 1
