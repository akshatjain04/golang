# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689745135.csv"
    fields:
      - "accountCode"
      - "profilePin"
  http:
    auth:
      type: "basic"
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            profileId: 767
            uuid: "uuidXYZ"
            # rest of the JSON payload goes here
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            profileId: 767
            # rest of the JSON payload with missing required fields goes here
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            profileId: 767
            uuid: "uuidXYZ with special characters or large data"
            # rest of the JSON payload goes here
          expect:
            - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          qs:
            query1: "value1"
            query2: "value2"
          json:
            profileId: 767
            uuid: "uuidXYZ"
            # rest of the JSON payload goes here
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            profileId: 767
            uuid: "uuidXYZ"
            # rest of the JSON payload goes here
          expect:
            - statusCode: 200
