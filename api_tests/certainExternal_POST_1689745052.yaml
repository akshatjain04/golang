# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689745052.csv"
    fields:
      - "accountCode"
      - "profileId"
      - "uuid"
      - "externalKey"
      - "firstName"
      - "lastName"
      - "email"
      - "username"
      - "password"
  defaults:
    headers:
      Content-Type: "application/json"
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          json:
            profileId: "{{ profileId }}"
            uuid: "{{ uuid }}"
            externalKey: "{{ externalKey }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          json:
            profileId: ""
            uuid: "{{ uuid }}"
            externalKey: "{{ externalKey }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          json:
            profileId: "{{ profileId }}"
            uuid: "{{ uuid }}"
            externalKey: "{{ externalKey }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
          expect:
            - statusCode: "{{ statusCode }}"

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}?profileIdentifier=SYNC_EXTERNAL"
          json:
            profileId: "{{ profileId }}"
            uuid: "{{ uuid }}"
            externalKey: "{{ externalKey }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          json:
            profileId: "{{ profileId }}"
            uuid: "{{ uuid }}"
            externalKey: "{{ externalKey }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
