# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2c3a7f4837

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields: ["petId"]
    skipHeader: true

scenarios:
  - name: "Pet ID edge cases"
    flow:
      - log: "Testing with invalid Pet ID"
        get:
          url: "/pet/INVALID_ID"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Testing with Pet ID not found"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
        think: 1
      - log: "Testing with invalid URL"
        get:
          url: "/pet/INVALID_URL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
      - log: "Testing with invalid Authorization token"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "INVALID_TOKEN"
          expect:
            - statusCode: 401
        think: 1
