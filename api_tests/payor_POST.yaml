# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    auth: "{{auth}}"
    trip_id: "2"
    signature: "/C:/Users/RAJAN KUMAR/Documents/1642515423324.jpg"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/payor-signature"
          headers:
            Authorization: "{{ auth }}"
            Content-Type: "application/json"
            Accept: "application/json"
          formData:
            trip_id: "{{ trip_id }}"
            signature: "{{ signature }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/payor-signature"
          headers:
            Authorization: "{{ auth }}"
            Content-Type: "application/json"
            Accept: "application/json"
          formData:
            trip_id: ""
            signature: "{{ signature }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/payor-signature"
          headers:
            Authorization: "{{ auth }}"
            Content-Type: "application/json"
            Accept: "application/json"
          formData:
            trip_id: "{{ trip_id }}"
            signature: "{{ signature }}"
          expect:
            - statusCode: "{{ responseStatusCode }}"
