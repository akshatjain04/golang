# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/certainExternal/service/v1/Profile"
  http:
    auth:
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"
  payload:
    path: "certainExternal_GET_1689771473.csv"
    fields:
      - "accountCode"
      
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ accountCode }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidaccountcode"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ accountCode }}?filterBy=specialcharacters"
          expect:
            - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/{{ accountCode }}?filterBy=value&orderBy=orderby1,orderby2"
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - get:
          url: "/{{ accountCode }}"
          expect:
            - statusCode: 200
