# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=8a6351bbc5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test /pet/findByStatus API for failure cases"
    flow:
      - think: 5
      - log: "Testing with invalid status value"
      - get:
          url: "/pet/findByStatus?status=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                description: "Invalid status value"
      - think: 5
      - log: "Testing with missing status query parameter"
      - get:
          url: "/pet/findByStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                description: "Invalid status value"
      - think: 5
      - log: "Testing with invalid authorization"
      - get:
          url: "/pet/findByStatus?status=available"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
      - think: 5
      - log: "Testing with invalid URL"
      - get:
          url: "/invalid_url?status=available"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
