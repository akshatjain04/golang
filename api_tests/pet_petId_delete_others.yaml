# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  processor: "./my-processor.js"

scenarios:
  - name: "Delete pet failure cases"
    flow:
      - log: "Test case: Attempt to delete pet with invalid petId"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "Bearer invalid_token"
            api_key: "invalid_api_key"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Test case: Attempt to delete pet without authorization"
        post:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.API_KEY }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      - log: "Test case: Attempt to delete pet with missing api_key"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Test case: Attempt to delete pet with invalid URL"
        post:
          url: "/invalid_url/{{ petId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ $processEnvironment.API_KEY }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
