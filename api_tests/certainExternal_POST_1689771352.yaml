# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689771352.csv"
    fields:
      - "accountCode"
      - "profilePin"
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            # valid JSON payload goes here
          expect:
            - statusCode: 200
            # Assertions for response body

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            # invalid JSON payload goes here
          expect:
            - statusCode: 400
            # Assertions for response body

  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            # JSON payload with edge case goes here
          expect:
            - statusCode: 200
            # Assertions for response body

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          qs:
            # Query parameters go here
          json:
            # valid JSON payload goes here
          expect:
            - statusCode: 200
            # Assertions for response body

  - name: "API request with authorization"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          json:
            # valid JSON payload goes here
          expect:
            - statusCode: 200
            # Assertions for response body
