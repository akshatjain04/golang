# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_GET_1689771601.csv"
    fields:
      - "accountCode"
      - "eventCode"
  http:
    auth:
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "id"

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}?queryParam=value"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"

  - name: "API request with authorization"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
