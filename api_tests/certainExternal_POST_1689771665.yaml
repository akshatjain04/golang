# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/certainExternal/service/v1/Event"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689771665.csv"
    fields:
      - "accountCode"
      - "eventCode"
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/{{ accountCode }}/{{ eventCode }}"
          json:
            # whole JSON payload as provided in the api spec goes here
          expect:
            - statusCode: 200
            # Add assertions for response body here

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/{{ accountCode }}/{{ eventCode }}"
          json:
            # Invalid JSON payload goes here
          expect:
            - statusCode: 400
            # Add assertions for error message here

  - name: "API request with edge case"
    flow:
      - post:
          url: "/{{ accountCode }}/{{ eventCode }}"
          json:
            # JSON payload with edge case goes here
          expect:
            # Add assertions for response status code and body here

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/{{ accountCode }}/{{ eventCode }}"
          qs:
            # Query parameters go here
          json:
            # Valid JSON payload goes here
          expect:
            - statusCode: 200
            # Add assertions for response body here

  - name: "API request with authorization"
    flow:
      - post:
          url: "/{{ accountCode }}/{{ eventCode }}"
          json:
            # Valid JSON payload goes here
          expect:
            - statusCode: 200
            # Add assertions for response body here
