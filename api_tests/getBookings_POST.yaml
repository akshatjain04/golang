# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    token: "{{token}}"
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/Booking/getBookings"
          headers:
            Authorization: "{{ token }}"
          json:
            driver_id: "181"
            timezone: "Asia/kolkata"
            vehicle_id: "143"
            current_trip_id: "146872"
            trip_date: "2023-05-13"
            switch_flag: "false"
            status_id: "-2"
          expect:
            - statusCode: 200
            # Add more assertions here to validate the response body

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/Booking/getBookings"
          headers:
            Authorization: "{{ token }}"
          json:
            driver_id: "" # Missing required field
            timezone: "Asia/kolkata"
            vehicle_id: "143"
            current_trip_id: "146872"
            trip_date: "2023-05-13"
            switch_flag: "false"
            status_id: "-2"
          expect:
            - statusCode: 400
            # Add more assertions here to validate the error message in the response body

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/Booking/getBookings"
          headers:
            Authorization: "{{ token }}"
          json:
            driver_id: "181"
            timezone: "Asia/kolkata"
            vehicle_id: "143"
            current_trip_id: "146872"
            trip_date: "2023-05-13"
            switch_flag: "false"
            status_id: "-2"
          # Add more assertions here to check the response status code and body
