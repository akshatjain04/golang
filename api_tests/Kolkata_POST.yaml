# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{url}}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "data.csv"
    fields:
      - "trip_id"
      - "flag"
      - "current_lat"
      - "current_lng"
      - "reasonText"
      - "distance"
      - "other_driver_id"
      - "sign_pic"
      - "driver_time"
      - "driver_timezone"
      - "driver_timezone_name"
  headers:
    Authorization: "MTI0XzIwMjMtMDYtMjkgMTA6MzY6MzY=649d4294d8f30"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/api/Booking/updateBookingStatus"
          json:
            trip_id: "{{ trip_id }}"
            flag: "{{ flag }}"
            current_lat: "{{ current_lat }}"
            current_lng: "{{ current_lng }}"
            reasonText: "{{ reasonText }}"
            distance: "{{ distance }}"
            other_driver_id: "{{ other_driver_id }}"
            sign_pic: "{{ sign_pic }}"
            driver_time: "{{ driver_time }}"
            driver_timezone: "{{ driver_timezone }}"
            driver_timezone_name: "{{ driver_timezone_name }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/api/Booking/updateBookingStatus"
          json:
            trip_id: ""
            flag: "{{ flag }}"
            current_lat: "{{ current_lat }}"
            current_lng: "{{ current_lng }}"
            reasonText: "{{ reasonText }}"
            distance: "{{ distance }}"
            other_driver_id: "{{ other_driver_id }}"
            sign_pic: "{{ sign_pic }}"
            driver_time: "{{ driver_time }}"
            driver_timezone: "{{ driver_timezone }}"
            driver_timezone_name: "{{ driver_timezone_name }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/api/Booking/updateBookingStatus"
          json:
            trip_id: "{{ trip_id }}"
            flag: "{{ flag }}"
            current_lat: "{{ current_lat }}"
            current_lng: "{{ current_lng }}"
            reasonText: "{{ reasonText }}"
            distance: "{{ distance }}"
            other_driver_id: "{{ other_driver_id }}"
            sign_pic: "{{ sign_pic }}"
            driver_time: "{{ driver_time }}"
            driver_timezone: "{{ driver_timezone }}"
            driver_timezone_name: "{{ driver_timezone_name }}"
          expect:
            - statusCode: 200
            - hasProperty: "data"
