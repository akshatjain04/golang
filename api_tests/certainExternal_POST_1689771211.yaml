# Test generated by RoostGPT for test postman-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/certainExternal/service/v1/Profile"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_POST_1689771211.csv"
    fields:
      - "accountCode"
      - "firstName"
      - "lastName"
      - "profilePin"
  processor: "./my_functions.js"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "1234567890123456789012345678901234567890" # 40 characters pin
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - post:
          url: "/{{ accountCode }}?profileIdentifier=SYNC_EXTERNAL"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - post:
          url: "/{{ accountCode }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          headers:
            Authorization: "Basic invalid_username:invalid_password"
          expect:
            - statusCode: 401
