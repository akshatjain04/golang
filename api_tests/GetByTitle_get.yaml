# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    appReg_bearerToken: "your_bearer_token"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/_api/web/lists/GetByTitle('Test list')?$select=ListItemEntityTypeFullName"
          headers:
            Authorization: "Bearer {{ appReg_bearerToken }}"
            Accept: "application/json;odata=nometadata"
          expect:
            - statusCode: 200
            - hasProperty: "ListItemEntityTypeFullName"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/_api/web/lists/GetByTitle('Test list')?$select=InvalidField"
          headers:
            Authorization: "Bearer {{ appReg_bearerToken }}"
            Accept: "application/json;odata=nometadata"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/_api/web/lists/GetByTitle('Test list')?$select=ListItemEntityTypeFullName"
          headers:
            Authorization: "Bearer {{ appReg_bearerToken }}"
            Accept: "application/json;odata=nometadata"
          expect:
            - statusCode: 200
            - hasProperty: "ListItemEntityTypeFullName"
