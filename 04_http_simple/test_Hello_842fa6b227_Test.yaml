# Test generated by RoostGPT for test golang-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://your-api-url.com"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/hello"
          headers:
            Content-Type: "text/html"
          expect:
            - statusCode: 200
            - hasProperty: "Hello"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/hello"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 415
  - name: "API request with edge case"
    flow:
      - get:
          url: "/hello"
          headers:
            Content-Type: "text/html"
            x-custom-header: "!@#$%^&*()"
          expect:
            - statusCode: 200
            - hasProperty: "Hello"
