# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    engine: "ws"
    flow:
      - send: '{"greeting": "Hello, Server!"}'
      - think: 1
      - assert:
          - statusCode: 200
          - body: "Hello, Client!"
    ws:
      subprotocols:
        - "json"

  - name: "API request with validation failure"
    engine: "ws"
    flow:
      - send: '{"notGreeting": "Hello, Server!"}'
      - think: 1
      - assert:
          - statusCode: 400
          - body: "error reading JSON: "
    ws:
      subprotocols:
        - "json"

  - name: "API request with edge case"
    engine: "ws"
    flow:
      - send: '{"greeting": "!@#$%^&*()_+~`"}'
      - think: 1
      - assert:
          - statusCode: "{{ 200 to 500 }}"
          - body: "Message Received: "
    ws:
      subprotocols:
        - "json"
