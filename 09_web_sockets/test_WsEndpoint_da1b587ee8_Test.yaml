# Test generated by RoostGPT for test golang-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  ws:
    # Use a relative URL if the target is specified in the config section
    url: "/socket"

scenarios:
  - name: "Successful API request"
    engine: "ws"
    flow:
      - send: '{"greeting": "Hello, Server!"}'
      - think: 1
      - receive:
          - match: '"Hello, Client!"'
      - assert:
          - statusCode: 200

  - name: "API request with validation failure"
    engine: "ws"
    flow:
      - send: '{"invalid_key": "Hello, Server!"}'
      - think: 1
      - receive:
          - match: 'error reading JSON'
      - assert:
          - statusCode: 400

  - name: "API request with edge case"
    engine: "ws"
    flow:
      - send: '{"greeting": "Hello, Server! This is a test message with special characters and large data. @#$%^&*()_+~`|{}[]:;<>?,./"}'
      - think: 1
      - receive:
          - match: '"Hello, Client!"'
      - assert:
          - statusCode: [200, 201, 202]
